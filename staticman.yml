allowedFields: ["name", "message", "post_id"]
requiredFields: ["name", "message", "post_id"]
commitMessage: "(Staticman) {fields.name}: {fields.message}"
format: json
moderation: false
branch: master
path: "docs/{fields.post_id}"
filename: "{@date:YYYY-mm-DD}-{@id}"
generatedFields:
  date:
    type: date
