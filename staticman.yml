allowedFields: ["name", "message", "postId", "replyTo"]
requiredFields: ["name", "message", "postId", "replyTo"]
commitMessage: "(Staticman) {fields.name}: {fields.message}"
format: json
moderation: false
branch: master
path: "raw/{fields.postId}"
filename: "{@date:YYYY-mm-DD}-{@id}"
generatedFields:
  date:
    type: date
